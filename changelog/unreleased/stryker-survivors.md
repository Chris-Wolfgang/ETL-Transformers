type: internal

Mutation-test hardening: redundant argument guards in the pipeline operators and `Then` removed, `Take` with a count of zero never touches the source, and new tests pin the throttle clock arithmetic, per-item cancellation and the chain's eager null check.
