type: internal

Code-scanning clean-up: the `CallerArgumentExpressionAttribute` polyfill carries a documented `ReSharper disable once CheckNamespace` (it must live in `System.Runtime.CompilerServices`). No behaviour change.
